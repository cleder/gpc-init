---
repos:
  - repo: https://github.com/rstcheck/rstcheck
    rev: v6.3.0 # should be replaced with the current version
    hooks:
      - id: rstcheck
        additional_dependencies: ['rstcheck[sphinx,toml]']
